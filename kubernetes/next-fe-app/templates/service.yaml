apiVersion: v1
kind: Service
metadata:
  name: {{ include "next-fe-app.fullname" . }}
  labels:
    {{- include "next-fe-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "next-fe-app.selectorLabels" . | nindent 4 }}
